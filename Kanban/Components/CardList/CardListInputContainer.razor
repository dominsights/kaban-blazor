@using MudBlazor

<MudCollapse Expanded=@(!open)>
    <CardListInputCard setOpen=setOpen OnClick=AddCardList></CardListInputCard>
</MudCollapse>
<MudCollapse Expanded=@open>
    <MudPaper Elevation=0
              @onclick=@(() => setOpen(!open))>
              <MudText>+ Add a Card List</MudText>
    </MudPaper>
</MudCollapse>

@code {
    private bool open;
    private void setOpen(bool value)
    {
        open = value;
    }

    [Parameter] public EventCallback<string> AddCardList { get; set; }
}
